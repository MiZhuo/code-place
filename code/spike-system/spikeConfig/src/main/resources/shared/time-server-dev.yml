server:
  port: 8002
eureka:
  instance:
    hostname: localhost
    port: 8001
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${eureka.instance.port}/eureka/